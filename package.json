{
  "name": "greendays",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build && wrangler dev --port 8787",
    "build": "vite build",
    "eval": "python3 eval/grade.py --no-judge --out eval/runs/local-$(date +%F).json",
    "predeploy": "bash scripts/predeploy-eval.sh",
    "deploy": "npm run predeploy && wrangler deploy"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0",
    "wrangler": "^4.0.0"
  }
}
